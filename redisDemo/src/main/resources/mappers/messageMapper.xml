<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.redisdemo.mapper.MessageMapper">
    <resultMap id="MessageResultMap" type="Message">
        <id property="messageId" column="message_id"></id>
        <result property="cardId" column="card_id"></result>
        <result property="detail" column="detail"/>
        <result property="publicDate" column="public_date"/>
    </resultMap>
    <select id="selectMessage" resultMap="MessageResultMap">
        select message_id,card_id,detail,public_date from message
    </select>

    <insert id="insertMessage" useGeneratedKeys="true" keyProperty="messageId">
        insert into message(
        <if test="cardId!=null and cardId!=0"> card_id,</if>
        <if test="detail!=null and detail !=''">detail</if>
        public_date
        )values(
        <if test="cardId!=null and cardId!=0"> #{cardId},</if>
        <if test="detail!=null and detail !=''">#{detail},</if>
        sysdate()
        )
    </insert>
</mapper>